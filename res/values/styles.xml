<resources>
    <!-- <style name="AppTheme" parent="ThemeOverlay.AppCompat.ActionBar"> -->
    <!-- <style name="AppTheme" parent="Theme.AppCompat"> -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
        <!-- Customize your theme here. -->

        <item name="android:windowBackground">@color/main_window_BG_color</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">12pt</item>
        <!--<item name="android:textColor">@color/main_window_TXT_color</item>/>-->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="colorButtonNormal">@color/button_material_dark</item>
        <!--<item name="android:popupMenuStyle">@style/PopupMenu</item>-->
        <!--<item name="android:textAppearanceSmallPopupMenu">@style/myPopupMenuTextAppearanceSmall</item>-->
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
     </style>

    <!-- radiobutton style -->
    <style name="radionbutton"
        parent="Base.Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:button">@drawable/radiobutton_drawable</item>
    </style>

    <!-- popup menu style -->
    <!--
    <style name="PopupMenu"
        parent="@android:style/Widget.PopupMenu">
        <item name="android:textSize">20sp</item>
    </style>
    <style name="myPopupMenuTextAppearanceSmall"
        parent="@android:style/TextAppearance.DeviceDefault.Widget.PopupMenu.Small">
        <item name="android:textSize">15sp</item>
    </style>
    -->


</resources>
